{% extends 'base.html.twig' %}

{% block title %}Pista{% endblock %}

{% block body %}
    <h1>Pista</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pistum.id }}</td>
            </tr>
            <tr>
                <th>Num_pista</th>
                <td>{{ pistum.numPista }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pista_index') }}">back to list</a>

    <a href="{{ path('pista_edit', {'id': pistum.id}) }}">edit</a>

    {{ include('pista/_delete_form.html.twig') }}
{% endblock %}
